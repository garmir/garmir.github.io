name: Unique Test - garmir.github.io
on: [push, workflow_dispatch]

jobs:
  unique-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: DeterminateSystems/nix-installer-action@main
        continue-on-error: true

      - name: Unique Test Execution - garmir.github.io
        continue-on-error: true
        run: |
          nix-shell -p curl jq coreutils --run '
            echo "=== Unique Test: garmir.github.io ===" > unique-test-garmir.github.io.txt
            echo "Repository: ${{ github.repository }}" >> unique-test-garmir.github.io.txt
            echo "Test ID: TEST_157" >> unique-test-garmir.github.io.txt
            echo "Timestamp: $(date)" >> unique-test-garmir.github.io.txt
            curl -s ifconfig.me >> unique-test-garmir.github.io.txt
            echo "Unique instruction: General framework compliance and performance testing" >> unique-test-garmir.github.io.txt
          '

      - name: Ubuntu Fallback - garmir.github.io
        if: failure()
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y curl jq
          echo "Ubuntu fallback for garmir.github.io" > fallback-garmir.github.io.txt
          curl -s ifconfig.me >> fallback-garmir.github.io.txt

      - name: Upload Results
        uses: actions/upload-artifact@v4
        with:
          name: unique-test-garmir.github.io
          path: "*.txt"
          retention-days: 2